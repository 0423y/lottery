<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pfc.pfcl.infrastructure.dao.IStrategyDetailDao">


    <select id="queryStrategyDetailList" parameterType="java.lang.Long"
            resultType="com.pfc.pfcl.infrastructure.po.StrategyDetail">
        SELECT id, strategyId, awardId, awardCount, awardRate, createTime, updateTime
        FROM strategy_detail
        WHERE strategyId = #{strategyId}
    </select>
    <select id="queryNoStockStrategyAwardList" parameterType="java.lang.Long" resultType="java.lang.String">
        SELECT awardId
        FROM strategy_detail
        WHERE strategyId = #{strategyId}
          and awardSurplusCount = 0
    </select>



    <update id="deductStock" parameterType="com.pfc.pfcl.infrastructure.po.StrategyDetail">
        UPDATE strategy_detail
        set awardSurplusCount=awardSurplusCount - 1
        where strategyId = #{strategyId}
          and awardId = #{awardId}
          and awardSurplusCount > 0
    </update>


</mapper>
